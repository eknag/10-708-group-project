data_dir: "data/"

dataset_name: "CelebA"

model_name: "DVAE" 

model_config:
  latent_dim: 16
  sigma: 0.025

trainer_name: "BaseTrainer"
training_config:
  output_dir: "./results"
  learning_rate: 0.001
  batch_size: 100
  num_epochs: 100
  seed: 42





  
